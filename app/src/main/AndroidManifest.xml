<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.biocaribe_congresodermatologia_2019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_biocaribe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_biocaribe_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.PreguntaActivity"/>
            <activity android:name=".Activities.WinnerActivity"/>
        <activity android:name=".Activities.LoserActivity"/>
        <activity android:name=".Activities.ConfiguracionActivity"/>
    </application>

</manifest>